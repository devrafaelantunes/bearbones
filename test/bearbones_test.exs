defmodule BearbonesTest do
  use ExUnit.Case, async: false

  @tag :capture_log
  test "greets the world" do
    # {:ok, pid} = BB.Player.login("foodddd")

    # BB.Board.State.dump()

    # TestUtils.Board.reset()
  end
end
